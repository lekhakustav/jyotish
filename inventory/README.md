# Temple image inventory

Each approved image entry must include `media_id`, exact-file `sha256`, provenance, rights status, and `remaining_uses`. The scheduled check reports when remaining uses are at or below the one-day threshold. Generation/upload is not wired in this scaffold.
