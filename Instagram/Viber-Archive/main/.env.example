# Optional provider configuration; keep values out of Git.
# Deterministic-first means no provider call is needed for validation/report generation.
CONTENT_MODEL_PROVIDER=
CONTENT_MODEL=
TEMPLE_IMAGE_MODEL_PROVIDER=
TEMPLE_IMAGE_MODEL=
TEMPLE_ONE_DAY_THRESHOLD=1
