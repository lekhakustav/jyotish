# Raw aggregate exports

Files here are append-only and privacy-safe. Corrections receive a new `ingestion_id`, filename,
checksum, and row in `marketing/registry/ingestion-runs.csv`.

Large platform exports remain in Google Drive. Git may contain their metadata and small aggregate
snapshots, but never user-level event or audience-member data.
